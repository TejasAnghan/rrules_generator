name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rrule:
    git:
      url: git@github.com:3axislabs/rrule.git
      ref: main
  rrules_generator:
    path: ../

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  omni_datetime_picker: 0.1.2

flutter:
  uses-material-design: true
